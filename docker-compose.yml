version: "3.9"

services:
  api:
    build: .
    container_name: gto_poker_simulator
    ports:
      - "9000:9000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./static:/app/static
      
    command: >
      sh -c "PYTHONPATH=/app/src uvicorn api.main:app --host 0.0.0.0 --port 9000 --reload"


